import {TextStyle, ViewStyle} from 'react-native';

export type StyleSheetReturnType = {
  containerStyle: ViewStyle;
  titleCardStyle: ViewStyle;
  rightContainer: ViewStyle;
  nameStyle: TextStyle;
  roleStyles: TextStyle;
  titleStyle: TextStyle;
};
